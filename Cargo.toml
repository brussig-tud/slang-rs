[package]
name = "slang"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
slang-sys = {path="slang-sys"}

[features]
default = []
copy_libs = ["slang-sys/copy_libs"]

[workspace]
members = ["slang-sys"]
